# Variant patterns

## Web2

Search sibling endpoints, alternate HTTP methods, batch APIs, GraphQL resolvers, mobile/legacy APIs, background jobs, export/import paths, tenant/admin variants, and internal service wrappers.

## Smart contracts

Search inherited contracts, proxies/implementations, mirrored asset flows, callback paths, alternate token standards, share/asset conversions, emergency functions, governance executors, and cross-chain deployments.

## Rust / distributed systems

Search trait implementations, feature-gated implementations, unchecked conversions, `unwrap`/panic paths, message variants, epoch/round transitions, snapshot/replay/restart code, storage backends, and validation differences between ingress and replay.

## Arithmetic

Expand across overflow/underflow, truncation, signedness, rounding direction, precision scaling, zero denominator, stale accumulator, and order-of-operations differences.
